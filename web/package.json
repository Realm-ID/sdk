{
  "name": "@realm-id/web-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Browser SDK monorepo — core + React/Firebase/Google adapters",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
