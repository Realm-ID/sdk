{
  "name": "@realm-id/web-google",
  "version": "0.4.1",
  "private": true,
  "description": "Google Identity Services adapter for @realm-id/web — direct GIS popup/redirect (no Firebase).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx --test-reporter=spec src/*.test.ts"
  },
  "peerDependencies": {
    "@realm-id/web": "^0.4.0 || ^0.5.0"
  },
  "devDependencies": {
    "@realm-id/web": "^0.5.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
