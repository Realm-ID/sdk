{
  "name": "@realmid/web-firebase",
  "version": "0.4.0",
  "description": "Firebase Auth provider adapter for @realmid/web — popup + redirect kickoff for Google/email/phone.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@realmid/web": "^0.4.0",
    "firebase": ">=10"
  },
  "devDependencies": {
    "@realmid/web": "^0.4.0",
    "firebase": "^12.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
