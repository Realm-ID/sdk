{
  "name": "@realm-id/web-firebase",
  "version": "0.4.0",
  "description": "Firebase Auth provider adapter for @realm-id/web — popup + redirect kickoff for Google/email/phone.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@realm-id/web": "^0.4.0",
    "firebase": ">=10"
  },
  "devDependencies": {
    "@realm-id/web": "^0.4.0",
    "firebase": "^12.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
